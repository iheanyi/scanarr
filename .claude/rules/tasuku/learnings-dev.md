---
paths: bin/dev
---

# Tasuku Learnings

_Auto-synced from .tasuku/context/learnings.md_

## Rules

- Always run `sh -n bin/dev` after editing shell scripts; a Ruby-style `end` in a POSIX `if` must be `fi` and can otherwise surface only as a generic `unexpected end of file` parse error.

