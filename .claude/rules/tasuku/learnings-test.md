---
paths: test/**
---

# Tasuku Learnings

_Auto-synced from .tasuku/context/learnings.md_

## Insights

- On macOS with Ruby test parallelization, larger suites may intermittently fail with objc fork initialization errors; use `PARALLEL_WORKERS=1` for stable verification when this appears.

