# README

Developer docs:

- `docs/development.md` for local workflow and default dev test user seeding.
- `docs/self-hosting.md` for Docker Compose deployment, including local and S3-compatible media storage.
- `config/deploy.yml` for Kamal deployment defaults (including Redis, Sidekiq, and Active Storage runtime env).
