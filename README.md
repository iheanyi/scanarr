# README

Developer docs:

- `docs/development.md` for local workflow and default dev test user seeding.
- `docs/self-hosting.md` for Docker Compose deployment.
- `config/deploy.yml` for Kamal deployment defaults (including Redis + Sidekiq runtime env).
