#!/usr/bin/env ruby

require_relative "../config/environment"
require "sidekiq/cli"

unless ARGV.any? { |arg| arg == "-C" || arg.start_with?("--config") }
  ARGV.push("-C", "config/sidekiq.yml")
end

Sidekiq::CLI.instance.run
