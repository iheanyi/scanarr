<div class="<%= wrapper_classes %>"
     data-controller="multi-select"
     data-multi-select-all-label-value="<%= @all_label %>"
     data-multi-select-none-label-value="<%= none_label %>"
     data-multi-select-max-chips-value="<%= @max_chips %>">
  <div class="flex items-center justify-between">
    <% if @label.present? %>
      <span class="text-sm text-muted"><%= @label %></span>
    <% end %>
    <span class="text-xs text-muted-2" data-multi-select-target="count"><%= count_label %></span>
  </div>

  <div class="relative">
    <button type="button"
            class="flex w-full items-center justify-between gap-3 rounded-lg border border-border bg-surface-2 px-3 py-2 text-left text-sm text-foreground focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent"
            aria-controls="<%= panel_id %>"
            aria-expanded="false"
            data-action="click->multi-select#toggle"
            data-multi-select-target="trigger">
      <div class="flex flex-wrap items-center gap-1.5" data-multi-select-target="chips"></div>
      <span class="text-xs text-muted-2" data-multi-select-target="empty">Select options</span>
      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-muted-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
      </svg>
    </button>

    <div id="<%= panel_id %>"
         class="absolute z-10 mt-2 w-full rounded-lg border border-border bg-surface p-3 shadow-lg hidden"
         data-multi-select-target="panel">
      <input type="text"
             placeholder="<%= @filter_placeholder %>"
             class="w-full rounded-lg border border-border bg-surface-2 px-3 py-2 text-sm text-foreground placeholder:text-muted-2 focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent"
             data-multi-select-target="filter"
             data-action="input->multi-select#filter">

      <div class="mt-3 max-h-56 overflow-y-auto space-y-2">
        <% @options.each do |label, value| %>
          <label class="flex items-center gap-2 rounded-lg border border-border/80 bg-surface-2/60 px-3 py-2 text-sm text-foreground"
                 data-multi-select-target="option"
                 data-option-label="<%= label.to_s.downcase %>">
            <%= check_box_tag checkbox_name,
                              value,
                              @selected.include?(value.to_s),
                              class: "rounded border-border bg-surface-2 text-accent focus:ring-accent focus:ring-offset-surface-2",
                              data: {
                                multi_select_target: "checkbox",
                                action: "change->multi-select#changed",
                                label: label
                              } %>
            <span><%= label %></span>
          </label>
        <% end %>
      </div>
    </div>
  </div>

  <% if @hint.present? %>
    <p class="text-xs text-muted-2 text-pretty"><%= @hint %></p>
  <% end %>
</div>
