---
paths: config/tachiyomi_source_map.yml
---

# Tasuku Learnings

_Auto-synced from .tasuku/context/learnings.md_

## Insights

- Future adapter targets in `tachiyomi_source_map.yml` can go stale as keiyoushi/extensions index evolves; keep active targets in `future_sources` and move dead IDs to `retired_sources` so import diagnostics remain accurate without polluting adapter swarm priorities.

